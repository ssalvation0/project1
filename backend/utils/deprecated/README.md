# 📦 Deprecated API Methods

Ці файли містять старі методи роботи з Blizzard API та Wowhead scraping.

## 🗂️ Файли

### `blizzardAPI.js`
- **Призначення:** Робота з Blizzard Game Data API
- **Функції:**
  - `getAccessToken()` - OAuth авторизація
  - `getAllItemSets()` - Отримання списку сетів
  - `getItemSetDetails(setId)` - Деталі сету
  - `getItemMedia(itemId)` - Зображення предмета
- **Проблеми:**
  - Ліміти API (100 запитів/секунду)
  - Немає фільтрації по класах
  - Потребує credentials

### `wowheadParser.js`
- **Призначення:** Парсинг Wowhead для зображень сетів
- **Функції:**
  - `getWowheadSetImage(setId)` - Отримання зображення сету
- **Проблеми:**
  - Нестабільний HTML scraping
  - Може ламатися при змінах сайту
  - Повільний

### `wowheadItemsParser.js`
- **Призначення:** Парсинг Wowhead для списку предметів сету
- **Функції:**
  - `parseWowheadSetItems(setId)` - Список предметів
- **Проблеми:**
  - Те саме що wowheadParser

## 🔄 Чому замінено на Kaggle?

| Критерій | Старий підхід | Новий підхід (Kaggle) |
|----------|---------------|----------------------|
| **Швидкість** | Повільно (API запити) | Миттєво (локальні дані) |
| **Надійність** | Залежить від API | Офлайн, стабільно |
| **Фільтрація** | Обмежена | Повна (класи, якість, рівень) |
| **Кількість даних** | Обмежена API | 100,000+ предметів |
| **Залежності** | API ключі, інтернет | Тільки локальні файли |
| **Зображення** | API/scraping | Wowhead CDN (стабільний) |

## 🛠️ Як використовувати (якщо потрібно)

Якщо потрібно повернутися до старого підходу:

1. Скопіюй файли назад у `backend/utils/`:
```bash
cp deprecated/*.js ../
```

2. Оновити `routes/transmogs.js`:
```javascript
const { getItemSetDetails } = require('../utils/blizzardAPI');
const { getWowheadSetImage } = require('../utils/wowheadParser');
```

3. Додай `.env` credentials:
```
BLIZZARD_CLIENT_ID=твій_client_id
BLIZZARD_CLIENT_SECRET=твій_secret
BLIZZARD_REGION=eu
```

## 📝 Примітки

- Файли збережені для референсу та можливого використання
- Рекомендується використовувати новий підхід з Kaggle
- При необхідності можна комбінувати обидва підходи

## 🔗 Пов'язані файли

- Нова система: `backend/scripts/processKaggleData.js`
- Нова система: `backend/scripts/downloadImages.js`
- Інструкції: `backend/KAGGLE_SETUP.md`

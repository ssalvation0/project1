# 🗂️ Kaggle Dataset Integration Guide

Цей проект використовує [World of Warcraft Items Dataset](https://www.kaggle.com/datasets/trolukovich/world-of-warcraft-items-dataset/) з Kaggle для отримання даних про трансмог-сети.

## 📥 Крок 1: Завантаження датасету

1. Перейди на Kaggle: https://www.kaggle.com/datasets/trolukovich/world-of-warcraft-items-dataset/
2. Натисни **Download** (потрібен акаунт Kaggle)
3. Розархівуй `wowItems.csv`
4. Помісти файл у: `backend/data/kaggle/wowItems.csv`

```bash
# Структура:
backend/
  data/
    kaggle/
      wowItems.csv  ← сюди
```

## 🔧 Крок 2: Встановлення залежностей

```bash
cd backend
npm install csv-parser
```

## ⚙️ Крок 3: Обробка датасету

Запусти скрипт для конвертації CSV → JSON:

```bash
node scripts/processKaggleData.js
```

**Що робить скрипт:**
- Читає `wowItems.csv`
- Фільтрує тільки armor предмети
- Групує по сетах
- Витягує класи, рівні, якість
- Зберігає у `backend/data/transmog_sets.json`

**Очікуваний результат:**
```
🚀 Початок обробки Kaggle датасету...
📦 Оброблено 10000 предметів...
📦 Оброблено 20000 предметів...
...
✅ Завершено обробку 100000+ предметів
📊 Знайдено 500+ унікальних сетів
💾 Дані збережено у: backend/data/transmog_sets.json
```

## 🖼️ Крок 4: Завантаження зображень

Завантаж іконки предметів з Wowhead:

```bash
node scripts/downloadImages.js
```

**Що робить скрипт:**
- Читає `transmog_sets.json`
- Для кожного предмета завантажує іконку з Wowhead
- Зберігає у `backend/public/images/items/`
- Показує прогрес

**Параметри:**
- Розмір іконок: 256x256 (large) → fallback 56x56 (medium)
- Затримка між запитами: 150ms
- Батч розмір: 50 предметів

**Очікуваний результат:**
```
🖼️  Початок завантаження зображень...
📊 Всього унікальних предметів: 2500
⏳ Прогрес: 25% | ✅ 500 | ⏭️  100 | ❌ 5
⏳ Прогрес: 50% | ✅ 1000 | ⏭️  200 | ❌ 10
...
✨ Завершено!
✅ Завантажено: 2300
⏭️  Пропущено (вже є): 180
❌ Помилок: 20
```

## 🚀 Крок 5: Запуск серверу

```bash
npm start
```

API тепер працює з локальними даними Kaggle:
- `GET /api/transmogs` - список сетів з фільтрацією
- `GET /api/transmogs/:id` - деталі сету

## 📊 Структура даних

### `transmog_sets.json`

```json
[
  {
    "id": "set-1",
    "name": "Tier 2 - Judgment",
    "classes": ["Paladin"],
    "minLevel": 60,
    "maxLevel": 60,
    "quality": "Epic",
    "itemCount": 8,
    "items": [
      {
        "id": 16955,
        "name": "Judgment Crown",
        "slot": "Head",
        "quality": "Epic",
        "itemLevel": 76,
        "requiredLevel": 60
      },
      // ... інші предмети
    ]
  }
]
```

### API Response

```json
{
  "transmogs": [
    {
      "id": "set-1",
      "name": "Tier 2 - Judgment",
      "iconUrl": "/images/items/16955.jpg",
      "imageUrl": "/images/items/16955.jpg",
      "class": "Paladin",
      "classes": ["Paladin"],
      "expansion": "Classic",
      "quality": "Epic",
      "itemCount": 8,
      "minLevel": 60,
      "maxLevel": 60,
      "items": [...]
    }
  ],
  "pagination": {
    "currentPage": 0,
    "totalItems": 500,
    "itemsPerPage": 20,
    "totalPages": 25
  }
}
```

## 🔄 Оновлення даних

Коли вийде новий датасет:

```bash
# 1. Завантаж новий wowItems.csv
# 2. Переоброби дані
node scripts/processKaggleData.js

# 3. Завантаж нові іконки (пропустить існуючі)
node scripts/downloadImages.js
```

## 🗂️ Архівовані методи (deprecated)

Старі API методи перенесені в `backend/utils/deprecated/`:
- `blizzardAPI.js` - Blizzard Game Data API
- `wowheadParser.js` - Wowhead scraping
- `wowheadItemsParser.js` - Wowhead item parsing

Ці файли збережені для можливого використання в майбутньому, але більше не використовуються в основному коді.

## ✅ Переваги нового підходу

- ✅ **Офлайн робота** - не залежить від API лімітів
- ✅ **Швидкість** - дані локально
- ✅ **Повна фільтрація** - є інформація про класи
- ✅ **100,000+ предметів** - повна база
- ✅ **Контроль** - можна додавати свої поля

## 🛠️ Скрипти у package.json

```json
{
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "setup:kaggle": "node scripts/processKaggleData.js",
    "setup:images": "node scripts/downloadImages.js",
    "setup:all": "npm run setup:kaggle && npm run setup:images"
  }
}
```

Використання:
```bash
npm run setup:all  # Повна настройка (дані + зображення)
```

## 📝 Примітки

- CSV файл ~50MB
- JSON файл ~5MB
- Зображення ~500MB (при завантаженні всіх)
- Обробка CSV займає ~30 секунд
- Завантаження зображень ~10-30 хвилин (залежить від кількості)

## 🐛 Troubleshooting

**Помилка: `wowItems.csv не знайдено`**
- Переконайся що файл у правильній папці: `backend/data/kaggle/wowItems.csv`

**Помилка завантаження зображень: 429 Too Many Requests**
- Збільш затримку у `downloadImages.js`: `DELAY_MS = 300`

**Порожній результат API**
- Перевір чи існує `backend/data/transmog_sets.json`
- Запусти `node scripts/processKaggleData.js`

## 📚 Додаткові ресурси

- [Kaggle Dataset](https://www.kaggle.com/datasets/trolukovich/world-of-warcraft-items-dataset/)
- [Wowhead Icons](https://wow.zamimg.com/)
- [WoW API Documentation](https://develop.battle.net/documentation/world-of-warcraft)
